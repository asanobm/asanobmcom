/**
 * @Summary: This file contains all the constants used in the application.
 */
import path from "path";

export const BASE_PATH = '/posts'
export const POSTS_PATH = path.join(process.cwd(), BASE_PATH)